public class ternary {
    public static void main(String [] args) {
        int num = -20;
        String result;
        result = (num > 0) ? "possitive number" : "negative number.";
        System.out.println(result);
    }
}
